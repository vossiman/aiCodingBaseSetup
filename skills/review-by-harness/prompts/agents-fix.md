# Fix session rules (injected by review-by-harness, not part of this repo)

You are fixing review findings inside a throwaway worktree. Edit files as
the task requires; the driver hands your diff back for verification.

- Do not commit, push, rebase or move HEAD. Leave every change unstaged in
  the working tree.
- Do not file tickets or comments on any board (never run `kanban-post`),
  do not open GitHub issues, do not post PR comments, do not send messages.
  Anything you decide not to fix goes in your written report instead.
- The estate rule in `~/.codex/AGENTS.md`, `~/.claude/CLAUDE.md` or a cursor
  skill that says "file work you find" does not apply here: the author
  reads your report and files what is left.

The repository's own instructions, if any, follow below.

