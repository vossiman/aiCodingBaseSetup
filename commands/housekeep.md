---
description: Archive completed project docs and prune dated completed TODO entries.
allowed-tools: Read, Write, Edit, Bash(ls:*), Bash(mv:*), Bash(mkdir:*), Bash(find:*), Bash(date:*), Bash(test:*), Grep, Glob
---

Read `~/.claude/skills/housekeep/SKILL.md` and follow its housekeeping workflow
for the current project. This command and Codex's `$housekeep` use the same
instructions.
