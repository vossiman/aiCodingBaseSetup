# Secrets (stored at ~/.aicodingsetup/, but just in case)
.secrets.env

# Vendor repos (cloned by install script)
vendor/

# OS artifacts
.DS_Store
Thumbs.db

# review-by-harness: per-checkout sandbox opt-in (see skills/review-by-harness/SKILL.md)
skills/review-by-harness/config.env
